spring:
  application:
    name: demo-gateway-provider-test
  devtools:
    restart:
      enabled: true
  servlet:
    multipart:
      max-file-size: 4GB
      max-request-size: 4GB
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848

server:
  port: 9090

logging:
  level:
    root: info
  file:
    name: /log/demo-gateway-provider-test.log
